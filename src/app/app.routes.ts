import { Routes } from '@angular/router';
import { CostSplitterComponent } from './cost-splitter/ui/cost-splitter.component';

export const routes: Routes = [
    {
         path: '',
        component: CostSplitterComponent
    }
];
